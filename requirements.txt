# 맛노래 - 음식이 노래하는 AI 숏폼 생성기
# 모든 모델 무료 로컬 실행

# Step 0: 배경 제거 (선택 - 없어도 동작)
rembg[cpu]>=2.0.50

# Step 1: Vision (BLIP 이미지 캡셔닝)
transformers>=4.36.0
torch>=2.0.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# Step 1+2: LLM (Ollama 로컬)
requests>=2.31.0

# Step 3: 보컬 (Bark - 무료, 노래 모드 지원) - 선택사항, 없으면 Edge TTS 사용
# pip install git+https://github.com/suno-ai/bark.git
# (GPU 권장, CPU에서도 동작 - 느림, 모델 약 5GB)

# Step 3 fallback: Voice (Edge TTS - 무료, 빠름)
edge-tts>=6.1.0
pydub>=0.25.1
scipy>=1.10.0

# Step 4+5: Animation + Rendering
moviepy>=2.0.0
numpy>=1.24.0

# Web Interface
flask>=3.0.0
werkzeug>=3.0.0
